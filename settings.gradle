pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS) // This enforces repositories to be defined here
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' } // For WebRTC or other custom dependencies
    }
}

rootProject.name = "BWComm"
include ':app'